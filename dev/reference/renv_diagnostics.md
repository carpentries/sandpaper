# Print a diagnostics report for the package cache

Print a diagnostics report for the package cache

## Usage

``` r
renv_diagnostics(path = ".", profile = "lesson-requirements")
```

## Arguments

- path:

  the path to the lesson to use for diagnostics

- profile:

  the profile to work with (defaults to "lesson-requirements"
