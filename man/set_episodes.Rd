% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_episodes.R
\name{set_episodes}
\alias{set_episodes}
\title{Set the schedule for the lesson}
\usage{
set_episodes(path, order = NULL, write = FALSE)
}
\arguments{
\item{path}{path to the lesson}

\item{order}{the episodes in the order presented (with extension)}

\item{write}{if \code{TRUE}, the schedule will overwrite the schedule in the
current file.}
}
\description{
Set the schedule for the lesson
}
\examples{

tmp <- tempfile()
create_lesson(tmp)
create_episode("using-R", path = tmp)
print(sched <- get_episodes(tmp))

# reverse the schedule
set_episodes(tmp, order = rev(sched))
# write it
set_episodes(tmp, order = rev(sched), write = TRUE)

# see it
get_episodes(tmp)

}
